name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Announce release on stegodon.social
        if: success()
        env:
          STEGODON_SSH_KEY: ${{ secrets.STEGODON_SSH_KEY }}
        run: |
          # Setup SSH key
          mkdir -p ~/.ssh
          echo "$STEGODON_SSH_KEY" > ~/.ssh/stegodon_key
          chmod 600 ~/.ssh/stegodon_key

          # Post announcement
          TAG_NAME="${GITHUB_REF#refs/tags/}"
          MESSAGE="🦣 Stegodon [$TAG_NAME](https://github.com/deemkeen/stegodon/releases/tag/$TAG_NAME) released!"

          echo "$MESSAGE" | ssh -o StrictHostKeyChecking=no \
            -i ~/.ssh/stegodon_key -p 37331 stegodon.social post -

          # Cleanup
          rm -f ~/.ssh/stegodon_key
